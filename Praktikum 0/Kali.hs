module Kali where
    -- DEFINISI DAN SPESIFIKASI
    kali :: Integer -> Integer -> Integer
        -- kali(x,y) menerima masukan 2 buah bilangan a, b dan menghasilkan nilai a * b
        -- a dan b merupakan integer
        
    -- REALISASI
    kali x y = x * y
